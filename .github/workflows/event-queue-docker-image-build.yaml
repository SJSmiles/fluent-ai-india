name: event-queue-docker-image-build

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    tags:
      - 'event-queue-*'  # This ensures the workflow is triggered only for tags starting with 'event-queue-'

  # Allow manually trigger
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Checkout to the branch
        uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '18.x'

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v3
        with:
          images: sj004/fluent-ai-india

      - name: Log in to container registry
        uses: docker/login-action@v1
        with:
          registry: docker.io
          username: sj004
          password: ${{ secrets.DOCKER_PASSWORD }}
          

      - name: Build and push container image to registry
        uses: docker/build-push-action@v4
        with:
          # platforms: linux/amd64,linux/arm64
          context: '{{defaultContext}}:event-queue/'
          #file: Dockerfile_cronapi
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
